import styled from "styled-components";

export const ContainerLink = styled.section `
    grid-area: section;
    margin: 2rem;
    padding: 3rem;
    background-color: #fff;
    border-radius: 1rem;
`