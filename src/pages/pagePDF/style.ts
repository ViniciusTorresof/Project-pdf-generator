import styled from "styled-components";

export const ContainerPDF = styled.section `
    grid-area: section;
    margin: 2rem;
    padding: 3rem;
    background-color: #fff;
    border-radius: 1rem;
`